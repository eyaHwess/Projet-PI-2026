# 🏗️ Architecture du Chat en Temps Réel

## 📊 Vue d'Ensemble

```
┌─────────────────────────────────────────────────────────────────┐
│                    SYSTÈME DE CHAT EN TEMPS RÉEL                │
│                                                                   │
│  Mode 1: POLLING (Actif)    │    Mode 2: MERCURE (Optionnel)   │
│  Latence: ~2s                │    Latence: <100ms                │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Mode 1: Architecture Polling (ACTIF)

```
┌──────────────┐                    ┌──────────────┐
│   Client 1   │                    │   Client 2   │
│  (Browser)   │                    │  (Browser)   │
└──────┬───────┘                    └──────┬───────┘
       │                                   │
       │ Polling toutes les 2s             │ Polling toutes les 2s
       │ GET /message/chatroom/{id}/fetch  │ GET /message/chatroom/{id}/fetch
       │                                   │
       ▼                                   ▼
┌─────────────────────────────────────────────────────────┐
│                    Symfony Server                        │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │         MessageController                       │    │
│  │                                                 │    │
│  │  • chatroom() - Affiche le chat                │    │
│  │  • fetchMessages() - Récupère nouveaux msgs    │    │
│  │  • sendVoiceMessage() - Envoie audio           │    │
│  └────────────────────────────────────────────────┘    │
│                         │                               │
│                         ▼                               │
│  ┌────────────────────────────────────────────────┐    │
│  │         EntityManager (Doctrine)                │    │
│  └────────────────────────────────────────────────┘    │
└──────────────────────────┬──────────────────────────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │   PostgreSQL    │
                  │    Database     │
                  │                 │
                  │  • messages     │
                  │  • users        │
                  │  • chatrooms    │
                  └─────────────────┘
```

### Flux de Données (Polling)

```
1. Envoi de Message
   ─────────────────
   Client 1 → [POST /message/chatroom/{id}] → Serveur → Database
                                                  ↓
                                            Message sauvegardé

2. Réception de Message
   ─────────────────────
   Client 2 → [GET /fetch?lastMessageId=X] → Serveur → Database
                                                  ↓
                                         Récupère nouveaux messages
                                                  ↓
                                            JSON Response
                                                  ↓
                                         Client 2 affiche
```

---

## 🚀 Mode 2: Architecture Mercure (OPTIONNEL)

```
┌──────────────┐                    ┌──────────────┐
│   Client 1   │                    │   Client 2   │
│  (Browser)   │                    │  (Browser)   │
└──────┬───────┘                    └──────┬───────┘
       │                                   │
       │ WebSocket Connection              │ WebSocket Connection
       │ Subscribe: chatroom/{id}          │ Subscribe: chatroom/{id}
       │                                   │
       ▼                                   ▼
┌─────────────────────────────────────────────────────────┐
│                    Mercure Hub                           │
│                  (Port 3000)                             │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │         WebSocket Manager                       │    │
│  │                                                 │    │
│  │  • Gère les connexions persistantes            │    │
│  │  • Broadcast les messages instantanément       │    │
│  │  • Gère les topics (chatroom/{id})             │    │
│  └────────────────────────────────────────────────┘    │
└──────────────────────────┬──────────────────────────────┘
                           │
                           │ HTTP POST (publish)
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                    Symfony Server                        │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │         MessageController                       │    │
│  │                                                 │    │
│  │  • chatroom() - Affiche le chat                │    │
│  │  • Publie via HubInterface                     │    │
│  │  • Génère HTML avec _message.html.twig         │    │
│  └────────────────────────────────────────────────┘    │
│                         │                               │
│                         ▼                               │
│  ┌────────────────────────────────────────────────┐    │
│  │         EntityManager (Doctrine)                │    │
│  └────────────────────────────────────────────────┘    │
└──────────────────────────┬──────────────────────────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │   PostgreSQL    │
                  │    Database     │
                  └─────────────────┘
```

### Flux de Données (Mercure)

```
1. Envoi de Message
   ─────────────────
   Client 1 → [POST /message/chatroom/{id}] → Serveur → Database
                                                  ↓
                                            Message sauvegardé
                                                  ↓
                                         Render _message.html.twig
                                                  ↓
                                         Publish to Mercure Hub
                                                  ↓
                                         Topic: chatroom/{id}

2. Réception Instantanée
   ──────────────────────
   Mercure Hub → [WebSocket Broadcast] → Client 2
                                             ↓
                                       Affichage instantané
                                       (< 100ms)
```

---

## 🔀 Architecture Hybride (Recommandée)

```
┌──────────────────────────────────────────────────────────────┐
│                    CLIENT (Browser)                           │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              JavaScript Layer                        │    │
│  │                                                      │    │
│  │  ┌──────────────┐         ┌──────────────┐         │    │
│  │  │   Mercure    │         │   Polling    │         │    │
│  │  │  (Primary)   │         │  (Fallback)  │         │    │
│  │  │              │         │              │         │    │
│  │  │  WebSocket   │         │  setInterval │         │    │
│  │  │  < 100ms     │         │  2000ms      │         │    │
│  │  └──────┬───────┘         └──────┬───────┘         │    │
│  │         │                        │                  │    │
│  │         └────────────┬───────────┘                  │    │
│  │                      │                              │    │
│  │                      ▼                              │    │
│  │         ┌────────────────────────┐                 │    │
│  │         │   Message Display      │                 │    │
│  │         │   (DOM Update)         │                 │    │
│  │         └────────────────────────┘                 │    │
│  └─────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────┐
        │  Mercure disponible?             │
        │                                  │
        │  OUI → Utilise WebSocket         │
        │  NON → Utilise Polling           │
        └──────────────────────────────────┘
```

---

## 📦 Structure des Composants

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND (Twig + JS)                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  templates/chatroom/                                         │
│  ├── chatroom_modern.html.twig  ← Template principal        │
│  └── _message.html.twig          ← Template partiel         │
│                                                              │
│  public/                                                     │
│  └── chatroom_dynamic.js         ← Polling + AJAX           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    BACKEND (Symfony)                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  src/Controller/                                             │
│  ├── MessageController.php       ← Gestion messages         │
│  ├── ChatroomController.php      ← Gestion chatrooms        │
│  └── GoalController.php          ← Gestion goals            │
│                                                              │
│  src/Entity/                                                 │
│  ├── Message.php                 ← Entité message           │
│  ├── Chatroom.php                ← Entité chatroom          │
│  ├── User.php                    ← Entité utilisateur       │
│  └── MessageReaction.php         ← Entité réaction          │
│                                                              │
│  src/Repository/                                             │
│  ├── MessageRepository.php                                  │
│  └── MessageReadReceiptRepository.php                       │
│                                                              │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE (PostgreSQL)                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Tables:                                                     │
│  ├── message                     ← Messages                 │
│  ├── chatroom                    ← Chatrooms                │
│  ├── user                        ← Utilisateurs             │
│  ├── message_reaction            ← Réactions                │
│  ├── message_read_receipt        ← Accusés de lecture       │
│  └── goal_participation          ← Participations           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Sécurité et Authentification

```
┌──────────────┐
│   Client     │
└──────┬───────┘
       │
       │ 1. Login
       ▼
┌─────────────────────────────────────┐
│     Symfony Security                │
│                                     │
│  • Authentification                 │
│  • Session Management               │
│  • CSRF Protection                  │
└──────────────┬──────────────────────┘
               │
               │ 2. JWT Token (Mercure)
               ▼
┌─────────────────────────────────────┐
│     Mercure Hub                     │
│                                     │
│  • Vérifie JWT                      │
│  • Autorise subscription            │
│  • Filtre par topic                 │
└─────────────────────────────────────┘
```

---

## 📊 Flux de Données Complet

```
┌─────────────────────────────────────────────────────────────┐
│                    ENVOI DE MESSAGE                          │
└─────────────────────────────────────────────────────────────┘

Client
  │
  │ 1. User tape message + sélectionne fichier
  │
  ▼
JavaScript (chatroom_dynamic.js)
  │
  │ 2. Validation (texte OU fichier)
  │
  ▼
FormData
  │
  │ 3. POST /message/chatroom/{id}
  │    Content-Type: multipart/form-data
  │
  ▼
MessageController::chatroom()
  │
  │ 4. Validation formulaire
  │ 5. Upload fichier (si présent)
  │ 6. Création entité Message
  │
  ▼
EntityManager
  │
  │ 7. persist() + flush()
  │
  ▼
PostgreSQL
  │
  │ 8. Message sauvegardé
  │
  ▼
MessageController (suite)
  │
  ├─────────────────┬─────────────────┐
  │                 │                 │
  ▼                 ▼                 ▼
Mercure Hub    JSON Response    Polling Fetch
(si activé)    (AJAX)           (automatique)
  │                 │                 │
  │                 │                 │
  ▼                 ▼                 ▼
Broadcast      Client 1         Client 2
instantané     reçoit           reçoit après 2s
< 100ms        confirmation
```

---

## 🎯 Points de Décision

```
┌─────────────────────────────────────────────────────────────┐
│              QUAND UTILISER QUEL MODE?                       │
└─────────────────────────────────────────────────────────────┘

Polling (Actuel)
├── ✅ < 100 utilisateurs simultanés
├── ✅ Pas de Docker disponible
├── ✅ Développement/Test
├── ✅ Latence 2s acceptable
└── ✅ Configuration simple

Mercure (Optionnel)
├── 🚀 > 100 utilisateurs simultanés
├── 🚀 Docker disponible
├── 🚀 Production
├── 🚀 Latence < 100ms requise
└── 🚀 Scalabilité importante

Hybride (Recommandé)
├── ✅ Mercure comme primary
├── ✅ Polling comme fallback
├── ✅ Meilleure expérience utilisateur
├── ✅ Robustesse maximale
└── ✅ Transition transparente
```

---

## 🔄 Cycle de Vie d'un Message

```
1. CRÉATION
   ────────
   User Input → Validation → FormData → AJAX POST

2. TRAITEMENT
   ──────────
   Controller → Entity → Doctrine → Database

3. PUBLICATION
   ───────────
   ┌─ Mercure Hub (si activé) → WebSocket Broadcast
   │
   └─ JSON Response → Client confirmation

4. RÉCEPTION
   ─────────
   ┌─ WebSocket Event → Instant Display (< 100ms)
   │
   └─ Polling Fetch → Display après 2s

5. AFFICHAGE
   ─────────
   Template Render → DOM Update → User voit message
```

---

## 📈 Métriques de Performance

```
┌─────────────────────────────────────────────────────────────┐
│                    COMPARAISON MODES                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Polling:                                                    │
│  ├── Latence: ████████████████████ 2000ms                   │
│  ├── CPU:     ████████░░░░░░░░░░░░ 40%                      │
│  ├── RAM:     ██████░░░░░░░░░░░░░░ 30%                      │
│  └── Network: ████████████░░░░░░░░ 60%                      │
│                                                              │
│  Mercure:                                                    │
│  ├── Latence: ██░░░░░░░░░░░░░░░░░░ 100ms                    │
│  ├── CPU:     ███░░░░░░░░░░░░░░░░░ 15%                      │
│  ├── RAM:     ████░░░░░░░░░░░░░░░░ 20%                      │
│  └── Network: ███░░░░░░░░░░░░░░░░░ 15%                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎉 Résumé

Le système utilise une **architecture hybride** qui combine:
- ✅ **Polling** (actif) - Fiable et simple
- 🚀 **Mercure** (optionnel) - Rapide et scalable

Cette approche garantit:
- 🔒 Robustesse (fallback automatique)
- ⚡ Performance (WebSocket quand disponible)
- 🎯 Flexibilité (activation progressive)
- 📈 Scalabilité (prêt pour la croissance)

**Le meilleur des deux mondes!** 🌟
