# Copier ce fichier en .env et remplir les valeurs (surtout les secrets).
# Ne jamais committer .env — il contient tes clés personnelles.
#
# cp .env.example .env   (Linux/Mac)
# copy .env.example .env (Windows)

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# Adapter l'URL à ta base locale (user, mot de passe, host, nom de la DB)
DATABASE_URL="postgresql://postgres:VOTRE_MOT_DE_PASSE@127.0.0.1:5432/pidev_db?serverVersion=16&charset=utf8"
###< doctrine/doctrine-bundle ###

###> symfony/messenger ###
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###

###> symfony/mailer ###
# Exemple : smtp://ton-email:mot-de-passe-app@smtp.gmail.com:587
MAILER_DSN=smtp://user:password@smtp.example.com:587
MAILER_FROM_EMAIL=example@example.com
MAILER_FROM_NAME=Mon App
###< symfony/mailer ###

###> Trello (optionnel) ###
TRELLO_API_KEY=
TRELLO_API_TOKEN=
###< Trello ###

###> Google OAuth2 (connexion avec Google) ###
# Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
###< Google OAuth2 ###

###> OpenAI (suggestions IA - optionnel) ###
# OPENAI_USE_MOCK=1 : suggestions fictives sans API (recommandé en dev)
OPENAI_API_KEY=
OPENAI_USE_MOCK=1
###< OpenAI ###
