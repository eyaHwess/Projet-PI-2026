framework:
    notifier:
        # Configure admin recipients for notifications
        admin_recipients:
            - { email: admin@buildify.com }
        
        # Configure notification channels
        channel_policy:
            # urgent: ['sms', 'email']
            # high: ['email']
            high: ['email']
            medium: ['email']
            low: ['email']
        
        # Configure transports (optional - for SMS, Slack, etc.)
        # chatter_transports:
        #     slack: '%env(SLACK_DSN)%'
        # texter_transports:
        #     twilio: '%env(TWILIO_DSN)%'
