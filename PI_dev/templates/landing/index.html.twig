{% extends 'base.html.twig' %}

{% block title %}MyRoutine{% endblock %}

{% block body %}
<div class="bg-slate-50 text-gray-700 min-h-screen">

    <!-- NAVBAR -->
    <nav class="max-w-7xl mx-auto px-6 py-6 flex justify-between items-center">
        <div class="flex items-center gap-2 text-xl font-bold">
            🌸 <span>MyRoutine</span>
        </div>

        <div class="space-x-4">
            <a href="{{ path('app_login') }}"
               class="text-gray-600 hover:text-blue-400 font-medium">
                Login
            </a>
    
        </div>
    </nav>

    <!-- HERO -->
    <section class="max-w-7xl mx-auto px-6 py-24 grid md:grid-cols-2 gap-12 items-center">

        <!-- TEXT -->
        <div>
            <h1 class="text-4xl md:text-5xl font-bold leading-tight">
                Organize your routine,<br>
                <span class="text-rose-400">calmly</span>
            </h1>

            <p class="mt-6 text-gray-500 max-w-lg">
                A simple and balanced application to help you plan your days,
                build healthy habits, and stay focused without stress.
            </p>

            <div class="mt-8 flex gap-4">
              
                <a href="#features"
                   class="rounded-xl border border-blue-300 px-6 py-3 text-blue-400 hover:bg-blue-100 transition">
                    Learn more
                </a>
            </div>
        </div>

        <!-- ILLUSTRATION (ABSTRACT / NEUTRAL) -->
        <div class="flex justify-center">
            <div class="w-64 h-64 md:w-80 md:h-80 rounded-full
                        bg-gradient-to-br from-blue-100 to-rose-100
                        flex items-center justify-center shadow-xl">

                <img
                    src="/images/wom.png"
                    alt="Routine illustration"
                    class="w-3/4 h-3/4 object-contain"
                >
            </div>
        </div>

    </section>

    <!-- FEATURES -->
    <section id="features" class="max-w-7xl mx-auto px-6 py-20 grid md:grid-cols-3 gap-8">

        <div class="bg-white rounded-3xl p-6 shadow text-center">
            <div class="text-4xl mb-3">📅</div>
            <h3 class="font-semibold text-lg">Daily planning</h3>
            <p class="text-sm text-gray-500 mt-2">
                Structure your days with clarity and flexibility.
            </p>
        </div>

        <div class="bg-white rounded-3xl p-6 shadow text-center">
            <div class="text-4xl mb-3">⭐</div>
            <h3 class="font-semibold text-lg">Habit building</h3>
            <p class="text-sm text-gray-500 mt-2">
                Build habits progressively, at your own rhythm.
            </p>
        </div>

        <div class="bg-white rounded-3xl p-6 shadow text-center">
            <div class="text-4xl mb-3">⚖️</div>
            <h3 class="font-semibold text-lg">Balance & focus</h3>
            <p class="text-sm text-gray-500 mt-2">
                Stay consistent without pressure or overload.
            </p>
        </div>

    </section>

    <!-- VALUE SECTION -->
    <section class="bg-white py-24">
        <div class="max-w-4xl mx-auto px-6 text-center">
            <h2 class="text-3xl font-bold">
                Built for everyone
            </h2>
            <p class="mt-4 text-gray-500">
                MyRoutine is designed to support all users in creating
                sustainable routines, regardless of lifestyle or goals.
            </p>
        </div>
    </section>

    <!-- CTA -->
    <section class="py-24 text-center">
        <h2 class="text-3xl font-bold">
            Start building better habits today
        </h2>
        <p class="mt-4 text-gray-500">
            Create your account and take control of your routine.
        </p>

      
    </section>

    <!-- FOOTER -->
    <footer class="bg-slate-100 py-6 text-center text-sm text-gray-500">
        © {{ "now"|date("Y") }} MyRoutine — Designed for balance
    </footer>

</div>
{% endblock %}
