{% extends 'base.html.twig' %}

{% block title %}Signaler un message{% endblock %}

{% block body %}
<style>
    .report-container {
        max-width: 600px;
        margin: 40px auto;
        padding: 20px;
    }

    .page-header {
        margin-bottom: 30px;
    }

    .page-header h1 {
        font-size: 28px;
        font-weight: 700;
        color: #050505;
        margin-bottom: 8px;
    }

    .page-header p {
        color: #65676b;
        font-size: 14px;
    }

    .message-preview {
        background: #f0f2f5;
        border-left: 3px solid #ff4444;
        padding: 16px;
        border-radius: 8px;
        margin-bottom: 24px;
    }

    .message-preview-header {
        display: flex;
        align-items: center;
        gap: 12px;
        margin-bottom: 12px;
    }

    .message-preview-avatar {
        width: 32px;
        height: 32px;
        border-radius: 50%;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        color: white;
        font-weight: 600;
        font-size: 13px;
    }

    .message-preview-author {
        font-weight: 600;
        color: #050505;
    }

    .message-preview-content {
        color: #050505;
        font-size: 14px;
        line-height: 1.5;
    }

    .form-card {
        background: white;
        border-radius: 12px;
        padding: 30px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .form-group {
        margin-bottom: 24px;
    }

    .form-group label {
        display: block;
        font-weight: 600;
        color: #050505;
        margin-bottom: 8px;
        font-size: 14px;
    }

    .form-control {
        width: 100%;
        padding: 12px 16px;
        border: 1px solid #e4e6eb;
        border-radius: 8px;
        font-size: 14px;
        transition: all 0.2s;
    }

    .form-control:focus {
        outline: none;
        border-color: #ff4444;
        box-shadow: 0 0 0 3px rgba(255, 68, 68, 0.1);
    }

    select.form-control {
        cursor: pointer;
    }

    textarea.form-control {
        resize: vertical;
        font-family: inherit;
    }

    .form-actions {
        display: flex;
        gap: 12px;
        justify-content: flex-end;
        margin-top: 30px;
    }

    .btn {
        padding: 12px 24px;
        border-radius: 8px;
        font-weight: 600;
        font-size: 14px;
        cursor: pointer;
        transition: all 0.2s;
        border: none;
        text-decoration: none;
        display: inline-block;
    }

    .btn-danger {
        background: #ff4444;
        color: white;
    }

    .btn-danger:hover {
        background: #e63939;
    }

    .btn-secondary {
        background: #e4e6eb;
        color: #050505;
    }

    .btn-secondary:hover {
        background: #d0d2d6;
    }

    .help-text {
        font-size: 12px;
        color: #65676b;
        margin-top: 4px;
    }

    .alert {
        padding: 12px 16px;
        border-radius: 8px;
        margin-bottom: 20px;
        font-size: 14px;
    }

    .alert-warning {
        background: #fff3cd;
        color: #856404;
        border: 1px solid #ffeaa7;
    }

    .alert-info {
        background: #d1ecf1;
        color: #0c5460;
        border: 1px solid #bee5eb;
    }
</style>

<div class="report-container">
    <div class="page-header">
        <h1>Signaler un message</h1>
        <p>Goal: {{ goal.title }}</p>
    </div>

    <div class="alert alert-info">
        <i class="fas fa-info-circle"></i>
        Votre signalement sera examiné par les modérateurs. Les signalements abusifs peuvent entraîner des sanctions.
    </div>

    <div class="message-preview">
        <div class="message-preview-header">
            <div class="message-preview-avatar">
                {{ message.author.firstName|first }}{{ message.author.lastName|first }}
            </div>
            <div class="message-preview-author">
                {{ message.author.firstName }} {{ message.author.lastName }}
            </div>
        </div>
        <div class="message-preview-content">
            {% if message.content %}
                {{ message.content|length > 200 ? message.content|slice(0, 200) ~ '...' : message.content }}
            {% else %}
                <em>[Message avec pièce jointe]</em>
            {% endif %}
        </div>
    </div>

    <div class="form-card">
        {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.reason) }}
                {{ form_widget(form.reason) }}
                {{ form_errors(form.reason) }}
                <div class="help-text">Sélectionnez la raison principale du signalement</div>
            </div>

            <div class="form-group">
                {{ form_label(form.description) }}
                {{ form_widget(form.description) }}
                {{ form_errors(form.description) }}
                <div class="help-text">Fournissez des détails supplémentaires si nécessaire</div>
            </div>

            <div class="form-actions">
                <a href="{{ path('message_chatroom', {goalId: goal.id}) }}" class="btn btn-secondary">
                    Annuler
                </a>
                <button type="submit" class="btn btn-danger">
                    <i class="fas fa-flag"></i> Signaler ce message
                </button>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
