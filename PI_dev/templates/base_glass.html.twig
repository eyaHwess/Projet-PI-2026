<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Buildify{% endblock %}</title>
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --gradient-bg: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            --glass-bg: rgba(255, 255, 255, 0.15);
            --glass-border: rgba(255, 255, 255, 0.3);
            --text-dark: #2d3748;
            --text-light: #718096;
            --shadow-soft: 0 8px 32px rgba(31, 38, 135, 0.15);
            --shadow-hover: 0 12px 40px rgba(31, 38, 135, 0.25);
            
            /* Gradient colors */
            --gradient-purple: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --gradient-pink: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --gradient-blue: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            --gradient-cyan: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            --gradient-orange: linear-gradient(135deg, #fa709a 0%, #fee140 100%);
        }
        
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
            min-height: 100vh;
            color: var(--text-dark);
            overflow-x: hidden;
        }
        
        /* Glass Container */
        .glass-container {
            background: var(--glass-bg);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-radius: 24px;
            border: 1px solid var(--glass-border);
            box-shadow: var(--shadow-soft);
            padding: 2rem;
            transition: all 0.3s ease;
        }
        
        .glass-container:hover {
            box-shadow: var(--shadow-hover);
            transform: translateY(-2px);
        }
        
        /* Navbar */
        .navbar-glass {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(20px);
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.2);
        }
        
        .navbar-brand {
            font-size: 1.5rem;
            font-weight: 700;
            color: white;
            text-decoration: none;
        }
        
        .navbar-nav {
            display: flex;
            gap: 1rem;
            list-style: none;
            align-items: center;
        }
        
        .nav-link {
            color: rgba(255, 255, 255, 0.9);
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 12px;
            transition: all 0.3s ease;
            font-weight: 500;
        }
        
        .nav-link:hover {
            background: rgba(255, 255, 255, 0.2);
            color: white;
        }
        
        /* Buttons */
        .btn-glass {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 50px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
        
        .btn-glass:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
        }
        
        .btn-gradient-purple {
            background: var(--gradient-purple);
            border: none;
            color: white;
        }
        
        .btn-gradient-pink {
            background: var(--gradient-pink);
            border: none;
            color: white;
        }
        
        .btn-gradient-blue {
            background: var(--gradient-blue);
            border: none;
            color: white;
        }
        
        .btn-gradient-cyan {
            background: var(--gradient-cyan);
            border: none;
            color: white;
        }
        
        /* Cards */
        .card-glass {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(20px);
            border-radius: 20px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 1.5rem;
            box-shadow: var(--shadow-soft);
            transition: all 0.3s ease;
        }
        
        .card-glass:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-hover);
        }
        
        /* Icon Buttons */
        .icon-btn {
            width: 48px;
            height: 48px;
            border-radius: 16px;
            background: rgba(255, 255, 255, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            color: white;
            font-size: 1.2rem;
        }
        
        .icon-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.1);
        }
        
        /* Search Bar */
        .search-glass {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 50px;
            padding: 0.75rem 1.5rem;
            color: white;
            width: 100%;
            max-width: 400px;
            outline: none;
            transition: all 0.3s ease;
        }
        
        .search-glass::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }
        
        .search-glass:focus {
            background: rgba(255, 255, 255, 0.25);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }
        
        /* Container */
        .container-fluid {
            padding: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }
        
        /* Grid */
        .grid-glass {
            display: grid;
            gap: 1.5rem;
        }
        
        .grid-2 { grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); }
        .grid-3 { grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); }
        .grid-4 { grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); }
        
        /* Typography */
        h1, h2, h3 {
            color: white;
            font-weight: 700;
        }
        
        h1 { font-size: 2.5rem; margin-bottom: 1rem; }
        h2 { font-size: 2rem; margin-bottom: 0.75rem; }
        h3 { font-size: 1.5rem; margin-bottom: 0.5rem; }
        
        p {
            color: rgba(255, 255, 255, 0.9);
            line-height: 1.6;
        }
        
        /* Badges */
        .badge-glass {
            background: rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.3);
            padding: 0.25rem 0.75rem;
            border-radius: 50px;
            font-size: 0.875rem;
            font-weight: 600;
            color: white;
            display: inline-block;
        }
        
        /* Progress Bar */
        .progress-glass {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50px;
            height: 8px;
            overflow: hidden;
        }
        
        .progress-fill {
            height: 100%;
            border-radius: 50px;
            transition: width 0.5s ease;
        }
        
        .progress-purple { background: var(--gradient-purple); }
        .progress-pink { background: var(--gradient-pink); }
        .progress-blue { background: var(--gradient-blue); }
        .progress-cyan { background: var(--gradient-cyan); }
        
        /* Responsive */
        @media (max-width: 768px) {
            .container-fluid {
                padding: 1rem;
            }
            
            h1 { font-size: 2rem; }
            h2 { font-size: 1.5rem; }
            
            .navbar-glass {
                padding: 1rem;
            }
        }
    </style>
    
    {% block stylesheets %}{% endblock %}
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar-glass">
        <a href="{{ path('app_home') }}" class="navbar-brand">
            <i class="bi bi-heart-fill"></i> Buildify
        </a>
        <ul class="navbar-nav">
            <li><a href="{{ path('app_goal_index') }}" class="nav-link">Objectifs</a></li>
            <li><a href="{{ path('app_routine_index') }}" class="nav-link">Routines</a></li>
            <li><a href="{{ path('app_activity_index') }}" class="nav-link">Activités</a></li>
            <li><a href="{{ path('app_calendar') }}" class="nav-link">Calendrier</a></li>
            {% block navbar_actions %}{% endblock %}
        </ul>
    </nav>
    
    <!-- Main Content -->
    <div class="container-fluid">
        {% block body %}{% endblock %}
    </div>
    
    {% block javascripts %}{% endblock %}
</body>
</html>
