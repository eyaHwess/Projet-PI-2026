{% extends 'base_modern.html.twig' %}

{% block title %}Nouvel Objectif{% endblock %}

{% block navbar_actions %}
    <a href="{{ path('app_goal_index') }}" class="btn-modern btn-modern-secondary">
        <i class="bi bi-arrow-left"></i> Retour
    </a>
{% endblock %}

{% block body %}
    <!-- AI Suggestion Banner -->
    {% if fromSuggestion is defined and fromSuggestion %}
    <div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: var(--radius-lg); padding: var(--space-lg); margin-bottom: var(--space-xl); color: white; box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);">
        <div style="display: flex; align-items: center; gap: var(--space-md);">
            <div style="width: 48px; height: 48px; background: rgba(255, 255, 255, 0.2); border-radius: var(--radius-md); display: flex; align-items: center; justify-content: center; font-size: 1.5rem; backdrop-filter: blur(10px);">
                🤖
            </div>
            <div style="flex: 1;">
                <h3 style="margin: 0 0 var(--space-xs) 0; font-size: var(--font-size-lg); font-weight: 700;">
                    <i class="bi bi-stars"></i> Objectif suggéré par l'IA
                </h3>
                <p style="margin: 0; opacity: 0.95; font-size: var(--font-size-sm);">
                    Les informations ci-dessous ont été pré-remplies à partir de votre suggestion IA. Vous pouvez les modifier selon vos besoins avant de créer l'objectif.
                </p>
            </div>
        </div>
    </div>
    {% endif %}
    
    <!-- Page Header -->
    <div class="page-header-modern">
        <h1 class="page-header-modern-title">
            <i class="bi bi-plus-circle" style="color: var(--primary-color);"></i>
            Nouvel Objectif
        </h1>
        <p class="page-header-modern-subtitle">
            Créez un nouvel objectif de fitness et bien-être
        </p>
    </div>

    <!-- Form Card -->
    <div class="card-modern" style="max-width: 800px; margin: 0 auto;">
        <div class="card-modern-header">
            <h2 style="font-size: var(--font-size-xl); font-weight: 700; margin: 0; color: var(--gray-900);">
                <i class="bi bi-bullseye"></i> Informations de l'objectif
            </h2>
        </div>

        {{ include('goal/_form.html.twig') }}
    </div>
{% endblock %}
