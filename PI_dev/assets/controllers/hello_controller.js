import { Controller } from '@hotwired/stimulus';

export default class extends Controller {
  connect() {
    // Intentionally empty: placeholder controller to ensure the controllers/ directory exists.
  }
}

