/*
 * Point d'entrée JavaScript (Asset Mapper).
 * Charge Stimulus pour les contrôleurs UX (ex: graphiques Chart.js).
 */
import { startStimulusApp } from '@symfony/stimulus-bundle';

startStimulusApp();
